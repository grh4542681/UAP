SET(EXECUTABLE_OUTPUT_PATH ${FRAM_BIN_PATH}/test)

SET(MESSAGE_TEST_SRC message_test.cc)
ADD_EXECUTABLE(message_test ${MESSAGE_TEST_SRC})

INCLUDE_DIRECTORIES("${FRAM_ROOT_PATH}/plat/infra/message/")
TARGET_LINK_LIBRARIES(message_test ${COMM_OS_LIB} ${PLAT_INFRA_LIB})
