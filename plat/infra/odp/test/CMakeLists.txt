SET(EXECUTABLE_OUTPUT_PATH ${FRAM_BIN_PATH}/test)
SET(SINGLETON_TEST_SRC singleton_test.cc)
SET(MULTITON_TEST_SRC multiton_test.cc)
add_executable(singleton_test ${SINGLETON_TEST_SRC})
add_executable(multiton_test ${MULTITON_TEST_SRC})
INCLUDE_DIRECTORIES("${FRAM_ROOT_PATH}/plat/infra/odp")
INCLUDE_DIRECTORIES("${FRAM_ROOT_PATH}/plat/infra/thread/mutex")
target_link_libraries(singleton_test libgthread.so libgprocess.so libgipc.so libgmp.so libgsocket.so  libgfile.so)
target_link_libraries(multiton_test libgthread.so libgprocess.so libgipc.so libgmp.so libgsocket.so libgfile.so)
