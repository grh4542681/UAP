--- #process config
process :
    group :
        switch : true
        size : 2

message :
    switch : true
    agent :
        name : PLAT_MESSAGE_DEAMON
        address : 
            protocol : local
            device : /plat/tmp/messaged.sock
        linksize : 100
    manager :
        address :
            protocol : local
            device : /plat/tmp/messaged.sock

mempool :
