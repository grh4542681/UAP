--- #process config
process :
    pool :
        switch : true
        minsize : 2
        maxsize : 10

message :
    switch : true
    agent :
        name : PLAT_MESSAGE_DEAMON
        address : 
            protocol : local
            device : /plat/tmp/messaged.sock
        linksize : 100
    manager :
        address :
            protocol : local
            device : /plat/tmp/messaged.sock

mempool :
