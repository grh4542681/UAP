--- #process config
process :
    group :
        switch : false
        size : 3

message :
    switch : true
    agent :
        name : PLAT_MESSAGE_DEAMON
        address : 
            protocol : local
            #device : /tmp/messaged.sock
            device : ${PLATTOP}
        linksize : 100
    manager :
        switch : true
        address :
            protocol : local
            device : /plat/tmp/messaged.sock

